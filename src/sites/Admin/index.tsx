import React from "react";

// TODO: Create a dashboard where you can see the number of users, number of posts, number of comments, etc.

const AdminPage: React.FC = () => {
    return (
        <div>
            AdminPage
        </div>
    )
}

export default AdminPage;
